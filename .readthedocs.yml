# http://docs.readthedocs.io/en/latest/yaml-config.html#requirements-file

# Don't build any extra formats
formats:
  - none

build:
  image: latest

python:
  version: 3.8
  pip_install: false
  extra_requirements:
    - docs